<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        body { font-family: Arial, sans-serif; background: #f8fafc; }
        .notfound-container {
            max-width: 500px;
            margin: 80px auto;
            background: #fff;
            border-radius: 14px;
            box-shadow: 0 6px 18px rgba(0,0,0,0.06);
            padding: 40px 32px;
            text-align: center;
        }
        .notfound-title { font-size: 2em; color: #dc2626; margin-bottom: 12px; }
        .notfound-msg { color: #64748b; font-size: 1.2em; margin-bottom: 24px; }
        .button { display: inline-block; padding: 10px 18px; border-radius: 10px; background: #0ea5e9; color: #fff; text-decoration: none; border: none; cursor: pointer; font-size: 1em; }
    </style>
</head>
<body>
    <div class="notfound-container">
        <div class="notfound-title">User Not Found</div>
        <div class="notfound-msg">Sorry, the user you are looking for does not exist or has been removed.</div>
        <a href="/" class="button">Back to Home</a>
    </div>
</body>
</html>
