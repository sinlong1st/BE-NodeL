<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle || "Welcome to My Cool App" %></title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&family=Pacifico&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <%= headerTitle || "Welcome!" %>
    </header>
    <main>
        <div class="card">
            <h2><%= cardTitle || "Dynamic Title Here!" %></h2>
            <p><%= cardContent || "This is a cool, dynamic EJS page. Replace this content with whatever you like!" %></p>
            <a href="<%= buttonLink || '#' %>" class="button"><%= buttonText || "Click Me!" %></a>
        </div>
    </main>
    <footer>
        Built by <%= author || "Default Admin" %>
    </footer>
</body>
</html>
